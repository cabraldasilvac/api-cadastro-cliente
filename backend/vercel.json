{
    "version": 2,
    "builds": [
        {
            "src": "./dist/api.ts",
            "use": "@now/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "./dist/api.ts"
        }
    ]
}
